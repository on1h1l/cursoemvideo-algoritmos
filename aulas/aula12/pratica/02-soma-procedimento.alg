algoritmo "soma_procedimento"
var
   x, y: Inteiro
procedimento soma(a, b: Inteiro)
inicio
      escrevaL("Valor de A:",a)
      escrevaL("Valor de B:",b)
      escrevaL("Soma entre A e B:",a+b)
fimProcedimento
inicio
      x <- 5
      y <- 3
      soma(x, y)
fimAlgoritmo