algoritmo "0_a_10"
var
   counter: inteiro
inicio
      counter <- 0
      
      enquanto (counter <= 10) faca
               escrevaL(counter)
               counter <- counter+1
      fimEnquanto
fimAlgoritmo
